Next JS:

Day-1
Learn about folder/directory bsae routing in next js. Next js provide the follwoing types of routing which are:
1. Simple Routing ----> /about
2. Nested Routing ---> /about/person1
3. Dynamic Routing-----[slugName]
4. Nested Dynamic Routing ----> [slugName]---->[slugName]
5. Catch All routes ---> [...slugName]
6. Optional Catch all routes ---> [[...slugName]] ---> this does not create or define on root level becasue this confilict with home page route
7. Private route ---> _folderName
8. skip or don't show in url ---> () 


Day-2

Rendering   paradigms
There are maninly tow types of rendering 
1. Server Side Rendering
    It has also tow type of rendering 
        i- Static site Rendering 
            --> Create a function and keep name generateStaticParams and return an array from this function 
            --> This is used when we have a Dynamic generate but frequently visited page we create during build time
            --> if we don't generate another page means we want to stop to generate after build then we export this: 
                        export const DynamicParams=false
        ii- Incremental Static Regeneration:
            --> if our page fetch new data after second then we use this rendering means in date and time,  weather website each request that give new or update time and date and weather then we use this in that component:
                export const revalidate="Time"
                e.g., export const revalidate= 5 ----> 5second
                
                Note: We only use ISG if we are working with SSG rendering otherwise not

2. Client Side Rendering
    This rendering use libraries like react where the server sent js code and our browser change them into html and css ....


@ How to create a Static page as Dynamic routes or page
ans-1: for this we write this line of code inside the static page in above:
    export dynamic="force-dynamic"
ans-2
function async dyn({searchParams}){
    const search =await searchParams; // this line also make the static page as dynamic means it does not create html file at build time
    return (
        <>
        </>
    )
}




Types of Component: 
there are tow type of component provide the next js 
1. Server Component (by default)
2. Client Component:
    To make the component as a client component we are use directives ("use client") at the top  of the compoenent

    directives: 
        There are tow type of directives provide next js: 
            i- use client
            ii- use server (don't required because compoenent are server component by default)


Day-3

How to fetch Data in srever and client component ?
Ans:
    In both component we can fetch data easy but there method is different , now we discuss in detail
    1.Client Component 
        In client component we take help hook of the react like useState and useEffect and there we use a directives which is must be required to declare in the top of the client component because the next js provide server component by default therefore we required this directives

        Note: 
            we cann't make the client component async there fore we use useEffect
    2. Server component



Day-4 

Parallel api fetching Vs Sequential api fetching

Sequential Api fetching:
 it is a type of fetching any api where we fetch api one by one means if the any fetching api data depends on other api data the we use Sequential api fetching technique

 diagramatic exmaple: 
 
    fetch("matric")
        |
        |
    fetch("Intermiediate")
        |
        |
    fetch("Graduate")
code exmaple:
const albumsResponse = await fetch(
    "https://jsonplaceholder.typicode.com/albums?_limit=10"
  );
  const albums = await albumsResponse.json();
  // console.log("A=lbums Data is: ", albums);

  const userResponse = await fetch(
    "https://jsonplaceholder.typicode.com/users?_limit=10"
  );
  const user = await userResponse.json();
  // console.log("User Data is: ", user);

  const todosResponse = await fetch(
    "https://jsonplaceholder.typicode.com/todos?_limit=10"
  );
  const todos = await todosResponse.json();
  // console.log("Todos Data is: ", todos);





